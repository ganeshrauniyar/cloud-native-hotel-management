apiVersion: v1
kind: Service
metadata:
  name: ibm-hotel-service
spec:
  type: NodePort
  selector:
    app: ibm-hotel
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
